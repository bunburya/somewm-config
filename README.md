# `somewm` config

This is my configuration for [SomeWM](https://somewm.org/), a Wayland compositor based on [AwesomeWM](https://awesomewm.org/). The main changes from the default config is the addition of some additional key-bindings and widgets.
